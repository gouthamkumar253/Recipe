
  
<div class="instrument-index-grid pt4">
  <% @recipe.each do |recipe| %>

   <div class="instrument border-light">
      <div class="instrument-thumb">
      		<%= link_to image_tag(recipe.image_url(:thumb)), recipe %>
	  </div>

    <div class="pa3">

      <h3 class="fw7 f4 title"><%= link_to recipe.title, recipe %></h3>
   </div>
  </div>
  <% end %>
  </div>